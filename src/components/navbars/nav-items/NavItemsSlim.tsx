// TODO(hb-migration): port the slim top-navbar items (`+NavbarIconsSlim` in
// phoenix-tailwind Mixins.pug). Not needed for the default vertical layout.
const NavItemsSlim = () => null;

export default NavItemsSlim;
