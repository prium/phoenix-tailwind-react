// TODO(hb-migration): port the support chat widget (`+SupportChat` in
// phoenix-tailwind SupportChat.pug, support-chat.css). Rendered as nothing
// until then so the layout shell stays free of react-bootstrap.
const ChatWidget = () => null;

export default ChatWidget;
